/**
 * Swiss locale and currency defaults (SSOT).
 *
 * Client-safe — no DB or server dependencies.
 * Import directly: `@kivvi/core/src/config/locale`
 */

/** Default locale for number/date formatting */
export const DEFAULT_LOCALE = "de-CH";

/** Default currency code */
export const DEFAULT_CURRENCY = "CHF";

/** Default country code (ISO 3166-1 alpha-2) */
export const DEFAULT_COUNTRY = "CH";
