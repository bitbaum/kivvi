{
  "root": true,
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-console": ["error", { "allow": ["warn", "error"] }]
  },
  "overrides": [
    {
      "files": ["e2e/**/*.ts"],
      "rules": { "no-console": "off" }
    }
  ]
}
